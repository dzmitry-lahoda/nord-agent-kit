# Nord protocol adapter

This directory supplies session execution and submission hooks not exposed by the pinned published SDK. Use public SDK exports and explicit dependencies; do not patch SDK internals or add tool/storage policy here.

See [the adapter boundary](../../docs/nord-adapter.md) for module responsibilities, dependencies and persistence contracts.
